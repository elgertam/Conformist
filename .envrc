# Automatically load the nix flake development environment
use flake

# Optional: Load any additional environment variables
# export CUSTOM_VAR="value"

# Set up development-specific paths
export NUGET_PACKAGES="$(pwd)/.nuget/packages"
export DOTNET_CLI_HOME=".dotnet-home"